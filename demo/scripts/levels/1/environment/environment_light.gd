@tool # <-- So it runs in the editor
extends DirectionalLight3D

func _ready():
	print("Environment Light script");
	#var new_transform: Transform3D;
	#self.set_param(Light3D.PARAM_ENERGY, 4.429);
	#new_transform.origin = Vector3(0, 1, 1.315);
	#new_transform.basis = Basis();
	#self.set_transform(new_transform);
	#self.set_rotation(Vector3(-68, 0, 0));
